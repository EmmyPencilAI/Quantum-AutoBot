{
  "entities": {
    "User": {
      "title": "User Profile",
      "description": "User profile data for leaderboard and community.",
      "type": "object",
      "properties": {
        "walletAddress": { "type": "string", "description": "Wallet address" },
        "username": { "type": "string", "description": "Display name" },
        "avatar": { "type": "string", "description": "Web3 avatar URL" },
        "totalProfit": { "type": "number", "description": "Total USDT profit" },
        "strategy": { "type": "string", "description": "Active strategy" },
        "region": { "type": "string", "description": "User region" },
        "lastActive": { "type": "string", "format": "date-time", "description": "Last activity timestamp" },
        "role": { "type": "string", "description": "User role" }
      },
      "required": ["walletAddress", "username", "totalProfit", "lastActive"]
    },
    "Post": {
      "title": "Community Post",
      "description": "A post in the community feed.",
      "type": "object",
      "properties": {
        "id": { "type": "string" },
        "authorUid": { "type": "string", "description": "Firebase UID" },
        "authorName": { "type": "string" },
        "authorAvatar": { "type": "string" },
        "authorWallet": { "type": "string", "description": "Wallet address" },
        "content": { "type": "string" },
        "likes": { "type": "number" },
        "createdAt": { "type": "string", "format": "date-time" }
      },
      "required": ["authorUid", "authorName", "authorWallet", "content", "likes", "createdAt"]
    },
    "Trade": {
      "title": "Trade History",
      "description": "History of trades for a user.",
      "type": "object",
      "properties": {
        "uid": { "type": "string" },
        "pair": { "type": "string" },
        "strategy": { "type": "string" },
        "pnl": { "type": "number", "description": "Total profit before split" },
        "userShare": { "type": "number", "description": "User's 50% share" },
        "timestamp": { "type": "string", "format": "date-time" }
      },
      "required": ["uid", "pair", "pnl", "userShare", "timestamp"]
    }
  },
  "firestore": {
    "/users/{uid}": {
      "schema": "User",
      "description": "User profiles"
    },
    "/posts/{postId}": {
      "schema": "Post",
      "description": "Community posts"
    },
    "/trades/{tradeId}": {
      "schema": "Trade",
      "description": "Trade history"
    }
  }
}
